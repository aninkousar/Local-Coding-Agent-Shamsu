requests>=2.31.0
rich>=13.7.0
PyYAML>=6.0.1
numpy>=1.26.0
flask>=3.0.0
pyflakes>=3.2.0

# Optional, only imported lazily when you actually open these file types.
# Install on demand: pip install pypdf python-docx
# pypdf>=4.0.0
# python-docx>=1.1.0

# Optional: database support beyond SQLite (SQLite itself needs nothing extra -
# it's Python's stdlib sqlite3 module).
# Install on demand: pip install psycopg2-binary   (Postgres)
# Install on demand: pip install pymysql            (MySQL)

# Optional but recommended: powers format_file and run_tests for Python projects.
# Install on demand: pip install black pytest
# black>=24.0.0
# pytest>=8.0.0

# Optional: SQL formatting for format_file (pure Python, no external binary needed).
# Install on demand: pip install sqlparse
# sqlparse>=0.5.0

# Optional: gives the GUI a real app window instead of opening in your browser.
# Install on demand: pip install pywebview
# pywebview>=5.0

